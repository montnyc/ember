{
  "name": "@montnyc/ember",
  "version": "0.10.0",
  "type": "module",
  "description": "PRD-driven autonomous coding loop tool",
  "bin": {
    "ember": "./dist/cli.js"
  },
  "files": [
    "dist/"
  ],
  "scripts": {
    "build": "bun build src/cli.ts --target=bun --outdir=dist --entry-naming=cli.js",
    "prepublishOnly": "bun run build",
    "test": "bun test",
    "typecheck": "bunx tsc --noEmit",
    "lint": "bunx oxlint src/",
    "fmt": "bunx oxfmt .",
    "fmt:check": "bunx oxfmt --check ."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/montnyc/ember.git"
  },
  "keywords": ["ralph-loop"],
  "author": "montnyc",
  "license": "MIT",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react": "^19.2.14",
    "oxlint": "latest",
    "typescript": "^5.8.0"
  },
  "dependencies": {
    "@opentui/core": "^0.1.90",
    "@opentui/react": "^0.1.90",
    "react": "^19.2.4"
  }
}
